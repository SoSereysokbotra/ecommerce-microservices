NODE_ENV=development
PORT=3002

# Neon PostgreSQL. Create a `catalog_db` database and paste the connection
# string into apps/catalog-service/.env — NEVER into this file, which is committed.
#
#   DATABASE_URL=postgresql://<user>:<password>@<endpoint>.neon.tech/catalog_db?sslmode=require
DATABASE_URL=

RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
RABBITMQ_EXCHANGE=commerce.events
RABBITMQ_QUEUE=catalog-service
REDIS_URL=redis://redis:6379

# Must match the gateway. Generate with: openssl rand -base64 48
JWT_SECRET=
