NODE_ENV=development
PORT=3005

# The M2 stub is stateless — no DATABASE_URL. M4 adds one along with Stripe.

# Set to true to force every charge to be declined. Used to demonstrate the
# failure path in docs/adr/0002-why-a-saga.md.
PAYMENTS_ALWAYS_DECLINE=false
