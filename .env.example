# Root compose environment. Copy to .env and fill in.
# NEVER put a real credential in .env.example — it is committed.

RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest

# Generate with: openssl rand -base64 48
JWT_SECRET=
